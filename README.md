To start the project run run_app.bat

To install the venv environment run inside the backend folder "python -m venv venv", ".\venv\Scripts\activate" and "pip install -r requirements.txt"
Add to the backend folder a .env file with HF_TOKEN=(Your HuggingFace token)
